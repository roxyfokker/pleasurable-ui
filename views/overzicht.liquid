{% render 'partials/head.liquid', page: page %}

<main class="overzicht">
  <div class="kop">
    <p>Instrument</p>
    <p>Status</p>
  </div>

  <ul class="lijst">
    {% for instrument in instruments %}
      <li class="rij">
        <p>{{ instrument.name }}</p>
        <p>{{ instrument.status }}</p>
      </li>
    {% endfor %}
  </ul>
</main>